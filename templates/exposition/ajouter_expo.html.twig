{% extends 'base.html.twig' %}

{% block title %}New Exposition{% endblock %}

{% block body %}
<div class="col-md-6 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Ajouter portfolio</h4>
            <p class="card-description"> Horizontal form layout </p>
    <h1>Create new Exposition</h1>

    {{ include('exposition/form_expo.html.twig') }}

    <a href="{{ path('liste_Exposition') }}">back to list</a>
    </div>
    </div>
    </div>
{% endblock %}
